return {
    SPAWN_INTERVAL_MIN = 15,   -- seconds, minimum gap between node spawns
    SPAWN_INTERVAL_MAX = 30,   -- seconds, maximum gap between node spawns
    LIFETIME            = 25,   -- seconds a node stays before despawning unclaimed
    PAYOUT_MIN           = 2000,
    PAYOUT_MAX           = 4000,
}