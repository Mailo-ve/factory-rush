-- UIConfig.lua
-- Owns: all UI display constants
-- Swap CURRENCY_SYMBOL and label strings when theme is decided

return {
    MONEY_UPDATE_INTERVAL   = 0.1,  -- how often UI refreshes money display
    LEADERBOARD_MAX_ENTRIES = 8,
    CURRENCY_SYMBOL         = "$",  -- placeholder, swap with theme
    INCOME_LABEL            = "per tick",   -- placeholder, swap with theme
    WIN_MESSAGE             = "wins the match!",
}