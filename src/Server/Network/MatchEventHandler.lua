-- MatchEventHandler.lua
-- Reserved for future client → server match communications
-- Currently a stub — MatchEvent only flows server → client in MVP
-- No logic lives here until a client-initiated match action is designed

local MachineEventHandler = {}

function MachineEventHandler.init()
    -- nothing to connect yet
end

return MachineEventHandler